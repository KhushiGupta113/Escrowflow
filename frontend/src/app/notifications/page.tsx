import { PageShell } from "@/components/page-shell";

export default function NotificationsPage() {
  return (
    <PageShell title="Notifications Center" subtitle="Live events for milestone, payment, and dispute updates.">
      <p className="text-slate-700">Realtime sockets support project/user channels and synchronized UI updates.</p>
    </PageShell>
  );
}

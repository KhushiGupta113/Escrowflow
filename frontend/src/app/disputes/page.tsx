import { PageShell } from "@/components/page-shell";

export default function DisputesPage() {
  return (
    <PageShell title="Dispute Center" subtitle="Raise issues with evidence and get admin moderation outcomes.">
      <p className="text-slate-700">APIs include dispute raise and admin resolution with release/refund outcome control.</p>
    </PageShell>
  );
}
